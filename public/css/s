<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shogun Command Center | AidFlow</title>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@500;700&family=Roboto:wght@300;400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="css/main.css">
    
    <style>
        /* ESTILOS EXCLUSIVOS DEL PANEL ADMIN */
        body { display: flex; height: 100vh; overflow: hidden; }
        
        /* Sidebar */
        .sidebar {
            width: 260px;
            background: #050505;
            border-right: 1px solid #333;
            display: flex;
            flex-direction: column;
            padding: 20px;
            flex-shrink: 0; /* Evita que se aplaste */
        }
        .brand {
            font-family: 'Orbitron';
            color: white;
            font-size: 1.2rem;
            margin-bottom: 40px;
            text-align: center;
            border-bottom: 2px solid var(--blood);
            padding-bottom: 10px;
        }
        .menu-item {
            padding: 15px;
            color: #888;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 15px;
            transition: 0.3s;
            border-radius: 8px;
            margin-bottom: 5px;
        }
        .menu-item:hover, .menu-item.active {
            background: rgba(142, 0, 0, 0.15);
            color: white;
            border-left: 4px solid var(--blood);
        }
        
        /* Main Content */
        .main-content {
            flex: 1;
            background: #0e0e0e;
            padding: 30px;
            overflow-y: auto;
        }

        /* Widgets de Métricas */
        .metrics-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }
        .metric-card {
            background: #151515;
            padding: 20px;
            border: 1px solid #333;
            border-radius: 8px;
            position: relative;
        }
        .metric-value { font-size: 2rem; font-family: 'Orbitron'; color: white; margin: 10px 0; }
        .metric-label { font-size: 0.8rem; color: #888; text-transform: uppercase; }
        .icon-bg { position: absolute; right: 15px; top: 15px; font-size: 2rem; opacity: 0.1; }

        /* Vistas (Tabs) */
        .view-section { display: none; animation: fadeIn 0.3s ease; }
        .view-section.active { display: block; }
        
        @keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }

        /* Sub-Tabs para Juegos */
        .sub-tabs { display: flex; gap: 10px; margin-bottom: 20px; border-bottom: 1px solid #333; padding-bottom: 10px; }
        .sub-tab { padding: 8px 15px; cursor: pointer; color: #666; font-size: 0.9rem; }
        .sub-tab.active { color: var(--gold); border-bottom: 2px solid var(--gold); font-weight: bold; }

        /* Tablas y Listas */
        .data-table { width: 100%; border-collapse: collapse; margin-top: 15px; }
        .data-table th { text-align: left; color: #666; padding: 10px; border-bottom: 1px solid #333; font-size: 0.8rem; }
        .data-table td { padding: 15px 10px; border-bottom: 1px solid #222; color: #ddd; }
        .btn-mini { padding: 5px 10px; font-size: 0.75rem; cursor: pointer; border-radius: 4px; border: none; }
        .bg-green { background: #1b4d1b; color: #4eff4e; }
        .bg-red { background: #4d1b1b; color: #ff4e4e; }

        /* 🔥 CORRECCIÓN VISUAL: Opciones horizontales */
        .checkbox-container {
            display: flex;
            align-items: center;
            gap: 20px;
            background: #111;
            padding: 12px;
            border: 1px solid #333;
            margin: 10px 0;
            flex-wrap: wrap;
        }
        /* Forzamos que los labels dentro de este contenedor sean inline-flex para ir al costado */
        .checkbox-container label {
            display: inline-flex !important; 
            align-items: center;
            margin-bottom: 0 !important;
            cursor: pointer;
            width: auto;
            color: #ddd;
            font-size: 0.9rem;
        }
        /* Ajustamos el checkbox input para que no ocupe 100% */
        .checkbox-container input[type="checkbox"] {
            width: auto !important;
            margin-right: 8px;
            accent-color: var(--blood);
        }
        .options-label {
            color: #666;
            font-size: 0.85rem;
            margin-right: 10px;
            font-weight: bold;
            text-transform: uppercase;
        }
    </style>
</head>
<body>

    <!-- 1. BARRA LATERAL -->
    <aside class="sidebar">
        <div class="brand">SHOGUN <span class="red-text">ADMIN</span></div>
        
        <div class="menu-item active" onclick="switchView('dashboard')">
            <i class="fas fa-chart-line"></i> Resumen Global
        </div>
        <div class="menu-item" onclick="switchView('tournaments')">
            <i class="fas fa-trophy"></i> Gestión Torneos
        </div>
        <div class="menu-item" onclick="switchView('games')">
            <i class="fas fa-gamepad"></i> Inventario Juegos
        </div>
        <div class="menu-item" onclick="switchView('users')">
            <i class="fas fa-users"></i> Usuarios & Refs
        </div>
        <div class="menu-item" onclick="switchView('dao')">
            <i class="fas fa-coins"></i> Fondo DAO
        </div>

        <div style="margin-top: auto;">
            <div class="menu-item" onclick="logout()">
                <i class="fas fa-sign-out-alt"></i> Cerrar Sesión
            </div>
        </div>
    </aside>

    <!-- 2. CONTENIDO PRINCIPAL -->
    <main class="main-content">
        
        <!-- VISTA: DASHBOARD -->
        <section id="view-dashboard" class="view-section active">
            <h2 class="section-title">VISIÓN GENERAL DEL DOJO</h2>
            <div class="metrics-grid">
                <div class="metric-card" style="border-top: 3px solid var(--gold);">
                    <div class="metric-label">Usuarios Totales</div>
                    <div class="metric-value" id="statUsers">0</div>
                    <i class="fas fa-users icon-bg"></i>
                </div>
                <div class="metric-card" style="border-top: 3px solid var(--blood);">
                    <div class="metric-label">Fondo DAO (Vivo)</div>
                    <div class="metric-value" id="statDAO">0 NC</div>
                    <i class="fas fa-university icon-bg"></i>
                </div>
                <div class="metric-card" style="border-top: 3px solid #4fdcff;">
                    <div class="metric-label">Torneos Activos</div>
                    <div class="metric-value" id="statActiveTournaments">0</div>
                    <i class="fas fa-trophy icon-bg"></i>
                </div>
                <div class="metric-card" style="border-top: 3px solid #00ff9c;">
                    <div class="metric-label">Ganancia Casa (Hoy)</div>
                    <div class="metric-value" id="statRevenue">0 NC</div>
                    <i class="fas fa-chart-pie icon-bg"></i>
                </div>
            </div>
        </section>

        <!-- VISTA: TORNEOS -->
        <section id="view-tournaments" class="view-section">
            <h2 class="section-title">GESTIÓN DE TORNEOS</h2>
            
            <div class="card-dark" style="margin-bottom: 20px;">
                <h3>🏆 Forjar Nuevo Torneo</h3>
                <div class="admin-form">
                    <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px;">
                        <input id="tName" type="text" placeholder="Nombre del Torneo">
                        <!-- Solo juegos internos -->
                        <select id="tGameSelect">
                            <option value="">Cargando juegos...</option>
                        </select>
                    </div>
                    
                    <div style="display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 15px; margin-top: 10px;">
                        <div>
                            <label class="muted-text">Entrada (NC)</label>
                            <input id="tFee" type="number" placeholder="0">
                        </div>
                        <div>
                            <label class="muted-text">Bolsa Premios (NC)</label>
                            <input id="tPrize" type="number" placeholder="1000">
                        </div>
                        <div>
                            <label class="muted-text">Ganadores (Top)</label>
                            <select id="tWinnersCount">
                                <option value="1">Solo el #1 (Winner takes all)</option>
                                <option value="3">Top 3 (Podio)</option>
                                <option value="20">Top 20</option>
                                <option value="50">Top 50</option>
                            </select>
                        </div>
                    </div>

                    <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px; margin-top: 10px;">
                        <div>
                            <label class="muted-text">Inicio</label>
                            <input id="tStart" type="datetime-local" style="width: 100%; background: #222; border: 1px solid #444; color: white; padding: 10px;">
                        </div>
                        <div>
                            <label class="muted-text">Cierre Automático</label>
                            <input id="tEnd" type="datetime-local" style="width: 100%; background: #222; border: 1px solid #444; color: white; padding: 10px;">
                        </div>
                    </div>

                    <button onclick="createTournament()" class="btn-ninja-primary" style="width: 100%; margin-top: 15px;">PUBLICAR TORNEO</button>
                </div>
            </div>

            <div id="tournamentsList"></div>
        </section>

        <!-- VISTA: JUEGOS (DIVIDIDA) -->
        <section id="view-games" class="view-section">
            <h2 class="section-title">INVENTARIO DEL DOJO</h2>
            
            <div class="sub-tabs">
                <div class="sub-tab active" onclick="switchGameTab('internal')">⚔️ SOFTWARE DEL CLAN</div>
                <div class="sub-tab" onclick="switchGameTab('external')">🌐 EMBEDS EXTERNOS</div>
            </div>

            <!-- Formulario Interno (Con Modos) -->
            <div id="form-internal" class="card-dark game-form-group">
                <h3>💾 Instalar Juego Oficial (Soporta Pagos)</h3>
                <div class="admin-form">
                    <input type="text" id="giTitle" placeholder="Título">
                    <input type="text" id="giImg" placeholder="Thumbnail URL">
                    <input type="text" id="giUrl" placeholder="Ruta: games/carpeta/index.html">
                    
                    <!-- CORRECCIÓN: Opciones al costado -->
                    <div class="checkbox-container">
                        <span class="options-label">MODOS PERMITIDOS:</span>
                        <label><input type="checkbox" id="modePractice" checked> Práctica</label>
                        <label><input type="checkbox" id="modeTournament" checked> Torneos</label>
                        <label><input type="checkbox" id="modeDuel"> Duelos</label>
                    </div>

                    <button onclick="createInternalGame()" class="btn-ninja-outline">INSTALAR</button>
                </div>
            </div>

            <!-- Formulario Externo (Solo Diversión) -->
            <div id="form-external" class="card-dark game-form-group" style="display:none;">
                <h3>🌍 Añadir Embed Externo (Solo Diversión)</h3>
                <div class="admin-form">
                    <input type="text" id="geTitle" placeholder="Título">
                    <input type="text" id="geImg" placeholder="Thumbnail URL">
                    <input type="text" id="geUrl" placeholder="URL Iframe (https://...)">
                    <button onclick="createExternalGame()" class="btn-ninja-outline" style="margin-top: 10px;">AÑADIR EMBED</button>
                </div>
            </div>
            
            <h3 class="gold-text" style="margin-top: 30px; font-size: 1rem;">LIBRERÍA ACTIVA</h3>
            <div id="gamesList" style="display: grid; grid-template-columns: repeat(auto-fill, minmax(200px, 1fr)); gap: 15px; margin-top: 15px;"></div>
        </section>

        <!-- VISTA: USUARIOS -->
        <section id="view-users" class="view-section">
            <h2 class="section-title">CENSO NINJA</h2>
            <div class="card-dark">
                <h3>🔥 Top Reclutadores</h3>
                <table class="data-table">
                    <thead><tr><th>Ninja</th><th>Nivel</th><th>Refs</th></tr></thead>
                    <tbody id="usersTableBody"></tbody>
                </table>
            </div>
        </section>

        <!-- VISTA: DAO -->
        <section id="view-dao" class="view-section">
            <h2 class="section-title">BÓVEDA DAO</h2>
            <div class="metrics-grid">
                <div class="metric-card"><div class="metric-label">Total</div><div class="metric-value gold-text">54,000 NC</div></div>
            </div>
        </section>

    </main>

    <script type="module" src="js/admin.js"></script>
    <script>
        function switchView(viewName) {
            document.querySelectorAll('.view-section').forEach(el => el.classList.remove('active'));
            document.querySelectorAll('.menu-item').forEach(el => el.classList.remove('active'));
            document.getElementById(`view-${viewName}`).classList.add('active');
            event.currentTarget.classList.add('active');
        }

        function switchGameTab(type) {
            document.querySelectorAll('.sub-tab').forEach(el => el.classList.remove('active'));
            event.currentTarget.classList.add('active');
            
            // Alternar formularios
            document.getElementById('form-internal').style.display = type === 'internal' ? 'block' : 'none';
            document.getElementById('form-external').style.display = type === 'external' ? 'block' : 'none';
            
            // Recargar lista filtrada
            window.filterGamesByType(type);
        }

        function logout() { localStorage.clear(); window.location.href = "login.html"; }
    </script>
</body>
</html>